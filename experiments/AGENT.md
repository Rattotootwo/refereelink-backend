# `experiments/` 与 `notebooks/` 协作说明

这里存放训练、分类实验和一次性分析，不是生产服务的默认入口。实验结果应记录数据、模型、设备、随机性/参数和输出位置；可复用逻辑成熟后再迁入 `app/`，并补生产测试。

不要让 notebook 或实验脚本悄悄改变生产模型、配置、数据库或已发布 Artifact。大模型权重、原始视频和本地输出遵守根目录忽略规则；需要加入仓库的结论应整理到 `docs/` 并注明证据范围。
